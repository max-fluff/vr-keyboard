name: package-updater
on: 
  push:
    branches:
      - master
jobs:
  update-package:
    runs-on: [self-hosted]
    steps:
      - run: git subtree push --prefix Assets/VRKeyboard-package origin upm
